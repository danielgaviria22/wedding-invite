export type TInviteData = {
  id: string;
  invitationRecipient: string;
  numberOfGuests: number;
};

export type TGuestInfo = {
  name: string;
  phone: string;
  email: string;
  transport: string;
  food: string;
  drink: string;
};
